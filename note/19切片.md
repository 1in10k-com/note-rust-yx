```
0000 切片slice是不持有所有权的数据类型

0150 iter和enumerate作用介绍

0450 字符串切片：字符串切片是指向字符串中一部分内容的引用
例子let hello = &s[0..5]   &s相当于是对s的引用，[0..5]表示引用的是字符串的一部分，包含0，但不包含5。


0845 &str 为字符串切片的符号

1219 字符串字面值其实就是字符串切片

1300 其它类型的切片
```